diff --git a/node_modules/expo-widget/android/src/main/java/expo/modules/expowidgets/ExpoWidgetsModule.kt b/node_modules/expo-widget/android/src/main/java/expo/modules/expowidgets/ExpoWidgetsModule.kt
index 8a014ea..0eb9eee 100644
--- a/node_modules/expo-widget/android/src/main/java/expo/modules/expowidgets/ExpoWidgetsModule.kt
+++ b/node_modules/expo-widget/android/src/main/java/expo/modules/expowidgets/ExpoWidgetsModule.kt
@@ -17,7 +17,28 @@ class ExpoWidgetsModule : Module() {
     Function("setWidgetData") { json: String, packageName: String -> 
       getPreferences(packageName).edit().putString("widgetdata", json).commit()
 
-      val intent = Intent(AppWidgetManager.ACTION_APPWIDGET_UPDATE)
+      // PATCH — tous les widgets poses finissaient par afficher le MEME contenu.
+      //
+      // Le code d'origine creait UN SEUL Intent hors de la boucle, puis pour
+      // chaque provider : ecrasait EXTRA_APPWIDGET_IDS et appelait
+      // sendBroadcast(intent) SANS destinataire. Deux fautes qui se combinent :
+      //
+      //   1. sans setComponent(), le broadcast part a TOUS les receivers de
+      //      l'app, pas au provider courant — `providerComponent` etait calcule
+      //      puis jamais utilise ;
+      //   2. le meme Intent etant reutilise, chaque tour ecrase les ids du
+      //      precedent.
+      //
+      // Resultat : chaque provider recevait les ids d'un AUTRE widget et
+      // redessinait ces cases avec SON propre layout. Le dernier provider de la
+      // liste gagnait. Comme « Mot du jour » est le seul a se reveiller seul
+      // (updatePeriodMillis=1800000, les autres sont a 0), c'est lui qui
+      // declenchait le plus souvent l'ecrasement — d'ou des widgets Serie et
+      // Ma semaine devenus « Mot du jour » au bout de quelques minutes
+      // (constate sur appareil, test interne).
+      //
+      // Specifique a Android : iOS passe par WidgetCenter.reloadAllTimelines(),
+      // ou chaque widget redessine le sien.
       val widgetManager = AppWidgetManager.getInstance(context)
       val widgetProviders = context.packageManager.queryBroadcastReceivers(
           Intent(AppWidgetManager.ACTION_APPWIDGET_UPDATE),
@@ -31,7 +52,15 @@ class ExpoWidgetsModule : Module() {
                   provider.activityInfo.name
               )
               val widgetIds = widgetManager.getAppWidgetIds(providerComponent)
-              intent.putExtra(AppWidgetManager.EXTRA_APPWIDGET_IDS, widgetIds)
+              // Rien a redessiner pour ce provider : ne pas diffuser un tableau
+              // vide, qui ferait repasser le widget par onUpdate sans ids.
+              if (widgetIds.isEmpty()) continue
+
+              // Un Intent NEUF par provider, ADRESSE a ce provider, ne portant
+              // que SES propres ids.
+              val intent = Intent(AppWidgetManager.ACTION_APPWIDGET_UPDATE)
+                  .setComponent(providerComponent)
+                  .putExtra(AppWidgetManager.EXTRA_APPWIDGET_IDS, widgetIds)
               context.sendBroadcast(intent)
           }
       }
diff --git a/node_modules/expo-widget/plugin/build/android/withModule.js b/node_modules/expo-widget/plugin/build/android/withModule.js
index ce45081..046e773 100644
--- a/node_modules/expo-widget/plugin/build/android/withModule.js
+++ b/node_modules/expo-widget/plugin/build/android/withModule.js
@@ -26,10 +26,15 @@ const withModule = (config, options) => {
             logger_1.Logging.logger.debug('moduleSourcePath: ', moduleSourcePath);
             const moduleDestinationPath = path_1.default.join(projectRoot, 'android/app/src/main/java', packageNameAsPath, 'Module.kt');
             if (!fs_extra_1.default.existsSync(moduleSourcePath)) {
-                logger_1.Logging.logger.debug('No module file found. Adding template...');
-                const contents = (0, module_template_1.getTemplate)(packageName);
-                logger_1.Logging.logger.debug('Writing contents');
-                fs_extra_1.default.writeFileSync(moduleDestinationPath, contents);
+                // PATCH : ne rien generer. Le template declare
+                // `expo.modules.widgets.ExpoWidgetsModule`, exactement la meme
+                // classe que celle deja compilee dans l'AAR du package npm et
+                // enregistree par l'autolinking (voir expo-module.config.json).
+                // La copie faisait donc doublon : sans minification Android le
+                // tolerait, mais R8 echoue avec
+                //   "Type expo.modules.widgets.ExpoWidgetsModule is defined multiple times".
+                // Le module du package suffit — il expose bien setWidgetData.
+                logger_1.Logging.logger.debug('No module file found. Skipping (package module is used instead).');
             }
             else {
                 fs_extra_1.default.copyFileSync(moduleSourcePath, moduleDestinationPath);
diff --git a/node_modules/expo-widget/plugin/build/android/withWidgetManifest.js b/node_modules/expo-widget/plugin/build/android/withWidgetManifest.js
index 6a50519..8c717ad 100644
--- a/node_modules/expo-widget/plugin/build/android/withWidgetManifest.js
+++ b/node_modules/expo-widget/plugin/build/android/withWidgetManifest.js
@@ -19,7 +19,10 @@ function buildWidgetReceiver(op) {
     return {
         $: {
             'android:name': `.${op.name}`,
-            'android:exported': 'false',
+            // PATCH: un widget d'ecran d'accueil DOIT etre exported=true, sinon
+            // le launcher (autre app) ne le voit pas et il n'apparait jamais
+            // dans la liste "Ajouter un widget". Le plugin mettait 'false'.
+            'android:exported': 'true',
         },
         'intent-filter': [
             {
diff --git a/node_modules/expo-widget/plugin/build/ios/withPodfile.js b/node_modules/expo-widget/plugin/build/ios/withPodfile.js
index e39bf98..f8648eb 100644
--- a/node_modules/expo-widget/plugin/build/ios/withPodfile.js
+++ b/node_modules/expo-widget/plugin/build/ios/withPodfile.js
@@ -62,16 +62,25 @@ target '${targetName}' do
   )
 end
 `;
-    const withAppExtFix = (0, generateCode_1.mergeContents)({
-        tag: "app_ext_fix",
-        src: podFileContent,
-        newSrc: `
+    // Ce correctif cible un motif ("resource_bundle_target.build_configurations.each
+    // do |config|") propre à un ancien template Podfile Expo/RN avec Sentry en
+    // ressource bundle — absent des Podfiles générés par les versions récentes
+    // d'Expo (SDK 54+), où mergeContents lève au lieu de no-op. On applique le
+    // fix uniquement quand l'ancre existe réellement, pour rester compatible
+    // sans dépendre du comportement legacy de generateCode.mergeContents.
+    const appExtFixAnchor = /resource_bundle_target.build_configurations.each do \|config\|/;
+    const withAppExtFix = appExtFixAnchor.test(podFileContent)
+        ? (0, generateCode_1.mergeContents)({
+            tag: "app_ext_fix",
+            src: podFileContent,
+            newSrc: `
         config.build_settings['APPLICATION_EXTENSION_API_ONLY'] = resource_bundle_target.name == 'Sentry' ? 'YES' : 'No'
         `,
-        anchor: /resource_bundle_target.build_configurations.each do \|config\|/,
-        offset: 1,
-        comment: "#",
-    });
+            anchor: appExtFixAnchor,
+            offset: 1,
+            comment: "#",
+        })
+        : { contents: podFileContent };
     const withAppExtFixPt2 = (0, generateCode_1.mergeContents)({
         tag: 'fix2',
         src: withAppExtFix.contents,
